{
  "name": "optisave-comercial-panel",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start:api": "node optisave-comercial-api/src/server.js",
    "docker:baileys": "docker compose up baileys tunnel --build",
    "docker:tunnel-quick": "docker compose --profile quick up baileys tunnel-quick --build"
  },
  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "@fastify/jwt": "^7.2.4",
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "fastify-plugin": "^4.5.1",
    "mongoose": "^8.5.1"
  }
}
